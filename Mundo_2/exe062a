print ('-=-' * 10)
print (f'{'GERADOR DE PA V3.0':^30}')
print ('-=-' * 10)
primeiro_termo = int(input('Primeiro termo: '))
razao = int(input('Digita a razão: '))

contador = 1
termo = primeiro_termo
mais = 10
total = 0

while mais != 0:
    total += mais
    while contador <= total:
        print(f'{termo} ->', end = ' ')
        contador += 1
        termo += razao
    print('PAUSA')
    mais = int(input('Quantos mais termos deseja adicionar? '))

print ('-=-' * 20)
print(f'Progressão aritmética finalizada com um total de {total} termos.')